@model FitnessCenterApp.Models.WorkingHours

@{
    ViewData["Title"] = "Çalışma Saati Ekle";
    var fitnessCenterId = ViewBag.FitnessCenterId;
    var daysOfWeek = ViewBag.DaysOfWeek as SelectList;
}

<h2>Çalışma Saati Ekle</h2>
<hr />

<div class="row">
    <div class="col-md-6">
        <form asp-action="AddWorkingHours">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="FitnessCenterId" value="@fitnessCenterId" />
            
            <div class="form-group mb-3">
                <label asp-for="DayOfWeek" class="control-label">Gün</label>
                <select asp-for="DayOfWeek" class="form-select" asp-items="daysOfWeek">
                    <option value="">Seçiniz</option>
                </select>
                <span asp-validation-for="DayOfWeek" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="OpeningTime" class="control-label">Açılış Saati</label>
                <input asp-for="OpeningTime" class="form-control" type="time" />
                <span asp-validation-for="OpeningTime" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="ClosingTime" class="control-label">Kapanış Saati</label>
                <input asp-for="ClosingTime" class="form-control" type="time" />
                <span asp-validation-for="ClosingTime" class="text-danger"></span>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save"></i> Kaydet
                </button>
                <a asp-action="WorkingHours" asp-route-id="@fitnessCenterId" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Geri Dön
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

